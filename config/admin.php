<?php

return [
    // The admin user's email address (set via .env)
    'email' => env('ADMIN_EMAIL'),
    // The admin user's password (set via .env - not set for production)
    'password' => env('ADMIN_PASSWORD'),
];
