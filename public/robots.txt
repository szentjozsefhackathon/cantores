User-agent: *

# Secret links are private by intent. The pages already send noindex; these
# rules also cover the file endpoints underneath them, which cannot.
Disallow: /s/
Disallow: /f/
Disallow: /p/
Disallow: /share/

# The pairing codes underneath /qr, but not /qr itself: that page has to be
# fetched for its noindex to be read at all.
Disallow: /qr/

Sitemap: https://cantores.hu/sitemap.xml
