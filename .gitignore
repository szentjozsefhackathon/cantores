/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
# Written by `octane:frankenphp` on first start, and copied in at build time by
# Dockerfile.prod. Either way it is generated, never edited.
/public/frankenphp-worker.php
# The FrankenPHP binary, if Octane is asked to download one locally. It is ~50MB
# and belongs to the machine, not to the repository.
/frankenphp
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed

.env.docker
.env.prod
.env.deploy
.editorconfig
/.devcontainer

AGENTS.md
boost.json
_ide_helper.php
mcp.json
/dist
.claude


# Cloudflare Origin CA certificate and key for Traefik; they live on the server only.
/certs
