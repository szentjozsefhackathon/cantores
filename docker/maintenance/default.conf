server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    return 503;
  }

  error_page 503 /index.html;

  location = /index.html {
    internal;
  }
}